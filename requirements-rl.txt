# Optional: Gymnasium + RL tooling (not required for core Haroma).
# Install: pip install -r requirements-rl.txt

gymnasium>=0.29.0

# Classic control / vector envs for SB3 experiments (optional)
# stable-baselines3>=2.0.0

# Used by mind/training/gymnasium_offline_bridge.train_linear_scorer_sklearn
scikit-learn>=1.3.0
